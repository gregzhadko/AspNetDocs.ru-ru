---
uid: signalr/overview/security/introduction-to-security
title: Введение в SignalR безопасности (ru) Документы Майкрософт
author: bradygaster
description: Описывает проблемы безопасности, которые необходимо учитывать при разработке приложения SignalR.
ms.author: bradyg
ms.date: 06/10/2014
ms.assetid: ed562717-8591-4936-8e10-c7e63dcb570a
msc.legacyurl: /signalr/overview/security/introduction-to-security
msc.type: authoredcontent
ms.openlocfilehash: 24ce20b45543468de28ad017ba62d2f6e5a00f3b
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/06/2020
ms.locfileid: "80675811"
---
# <a name="introduction-to-signalr-security"></a><span data-ttu-id="897d4-103">Общие сведения о безопасности SignalR</span><span class="sxs-lookup"><span data-stu-id="897d4-103">Introduction to SignalR Security</span></span>

<span data-ttu-id="897d4-104">[Патрик Флетчер](https://github.com/pfletcher), [Том Фицкакен](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="897d4-104">by [Patrick Fletcher](https://github.com/pfletcher), [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="897d4-105">В этой статье описаны проблемы безопасности, которые необходимо учитывать при разработке приложения SignalR.</span><span class="sxs-lookup"><span data-stu-id="897d4-105">This article describes the security issues you must consider when developing a SignalR application.</span></span>
>
> ## <a name="software-versions-used-in-this-topic"></a><span data-ttu-id="897d4-106">Версии программного обеспечения, используемые в этой теме</span><span class="sxs-lookup"><span data-stu-id="897d4-106">Software versions used in this topic</span></span>
>
>
> - [<span data-ttu-id="897d4-107">Visual Studio 2013</span><span class="sxs-lookup"><span data-stu-id="897d4-107">Visual Studio 2013</span></span>](https://my.visualstudio.com/Downloads?q=visual%20studio%202013)
> - <span data-ttu-id="897d4-108">.NET 4.5</span><span class="sxs-lookup"><span data-stu-id="897d4-108">.NET 4.5</span></span>
> - <span data-ttu-id="897d4-109">Версия SignalR 2</span><span class="sxs-lookup"><span data-stu-id="897d4-109">SignalR version 2</span></span>
>
>
>
> ## <a name="previous-versions-of-this-topic"></a><span data-ttu-id="897d4-110">Предыдущие версии этой темы</span><span class="sxs-lookup"><span data-stu-id="897d4-110">Previous versions of this topic</span></span>
>
> <span data-ttu-id="897d4-111">Для получения информации о более ранних версиях SignalR, см [SignalR Старые версии](../older-versions/index.md).</span><span class="sxs-lookup"><span data-stu-id="897d4-111">For information about earlier versions of SignalR, see [SignalR Older Versions](../older-versions/index.md).</span></span>
>
> ## <a name="questions-and-comments"></a><span data-ttu-id="897d4-112">Вопросы и комментарии</span><span class="sxs-lookup"><span data-stu-id="897d4-112">Questions and comments</span></span>
>
> <span data-ttu-id="897d4-113">Пожалуйста, оставьте обратную связь о том, как вам понравился этот учебник и что мы могли бы улучшить в комментариях в нижней части страницы.</span><span class="sxs-lookup"><span data-stu-id="897d4-113">Please leave feedback on how you liked this tutorial and what we could improve in the comments at the bottom of the page.</span></span> <span data-ttu-id="897d4-114">Если у вас есть вопросы, которые не имеют прямого отношения к учебнику, вы можете разместить их на [форуме ASP.NET SignalR](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) или [StackOverflow.com.](http://stackoverflow.com/)</span><span class="sxs-lookup"><span data-stu-id="897d4-114">If you have questions that are not directly related to the tutorial, you can post them to the [ASP.NET SignalR forum](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) or [StackOverflow.com](http://stackoverflow.com/).</span></span>

## <a name="overview"></a><span data-ttu-id="897d4-115">Обзор</span><span class="sxs-lookup"><span data-stu-id="897d4-115">Overview</span></span>

<span data-ttu-id="897d4-116">Этот документ содержит следующие разделы.</span><span class="sxs-lookup"><span data-stu-id="897d4-116">This document contains the following sections:</span></span>

- [<span data-ttu-id="897d4-117">Концепции безопасности SignalR</span><span class="sxs-lookup"><span data-stu-id="897d4-117">SignalR Security Concepts</span></span>](#concepts)

    - [<span data-ttu-id="897d4-118">Аутентификация и авторизация</span><span class="sxs-lookup"><span data-stu-id="897d4-118">Authentication and authorization</span></span>](#authentication)
    - [<span data-ttu-id="897d4-119">Токен подключения</span><span class="sxs-lookup"><span data-stu-id="897d4-119">Connection token</span></span>](#connectiontoken)
    - [<span data-ttu-id="897d4-120">Воссоединение групп при повторном подключении</span><span class="sxs-lookup"><span data-stu-id="897d4-120">Rejoining groups when reconnecting</span></span>](#rejoingroup)
- [<span data-ttu-id="897d4-121">Как SignalR предотвращает подделку запросов на перекрестные сайты</span><span class="sxs-lookup"><span data-stu-id="897d4-121">How SignalR prevents Cross-Site Request Forgery</span></span>](#csrf)
- [<span data-ttu-id="897d4-122">Рекомендации по безопасности SignalR</span><span class="sxs-lookup"><span data-stu-id="897d4-122">SignalR Security Recommendations</span></span>](#recommendations)

    - [<span data-ttu-id="897d4-123">Протокол «Безопасные слои розетки» (SSL)</span><span class="sxs-lookup"><span data-stu-id="897d4-123">Secure Socket Layers (SSL) protocol</span></span>](#ssl)
    - [<span data-ttu-id="897d4-124">Не используйте группы в качестве механизма безопасности</span><span class="sxs-lookup"><span data-stu-id="897d4-124">Do not use groups as a security mechanism</span></span>](#groupsecurity)
    - [<span data-ttu-id="897d4-125">Безопасная обработка входных данных от клиентов</span><span class="sxs-lookup"><span data-stu-id="897d4-125">Safely handling input from clients</span></span>](#input)
    - [<span data-ttu-id="897d4-126">Согласование изменения статуса пользователя с активным подключением</span><span class="sxs-lookup"><span data-stu-id="897d4-126">Reconciling a change in user status with an active connection</span></span>](#reconcile)
    - [<span data-ttu-id="897d4-127">Автоматически генерируемые прокси-файлы JavaScript</span><span class="sxs-lookup"><span data-stu-id="897d4-127">Automatically generated JavaScript proxy files</span></span>](#autogen)
    - [<span data-ttu-id="897d4-128">Исключения</span><span class="sxs-lookup"><span data-stu-id="897d4-128">Exceptions</span></span>](#exceptions)

<a id="concepts"></a>

## <a name="signalr-security-concepts"></a><span data-ttu-id="897d4-129">Концепции безопасности SignalR</span><span class="sxs-lookup"><span data-stu-id="897d4-129">SignalR Security Concepts</span></span>

<a id="authentication"></a>

### <a name="authentication-and-authorization"></a><span data-ttu-id="897d4-130">Аутентификация и авторизация</span><span class="sxs-lookup"><span data-stu-id="897d4-130">Authentication and authorization</span></span>

<span data-ttu-id="897d4-131">SignalR не предоставляет никаких функций для проверки подлинности пользователей.</span><span class="sxs-lookup"><span data-stu-id="897d4-131">SignalR does not provide any features for authenticating users.</span></span> <span data-ttu-id="897d4-132">Вместо этого вы интегрируете функции SignalR в существующую структуру аутентификации для приложения.</span><span class="sxs-lookup"><span data-stu-id="897d4-132">Instead, you integrate the SignalR features into the existing authentication structure for an application.</span></span> <span data-ttu-id="897d4-133">Вы проверяете подлинность пользователей, как обычно в вашем приложении, и работаете с результатами проверки подлинности в коде SignalR.</span><span class="sxs-lookup"><span data-stu-id="897d4-133">You authenticate users as you would normally in your application, and work with the results of the authentication in your SignalR code.</span></span> <span data-ttu-id="897d4-134">Например, можно проверить подлинность пользователей с помощью ASP.NET аутентификации форм, а затем в концентраторе, заставить пользователей или роли вызывать метод.</span><span class="sxs-lookup"><span data-stu-id="897d4-134">For example, you might authenticate your users with ASP.NET forms authentication, and then in your hub, enforce which users or roles are authorized to call a method.</span></span> <span data-ttu-id="897d4-135">В вашем концентраторе вы также можете передавать клиенту информацию о проверке подлинности, например, имя пользователя или принадлежность пользователя к роли.</span><span class="sxs-lookup"><span data-stu-id="897d4-135">In your hub, you can also pass authentication information, such as user name or whether a user belongs to a role, to the client.</span></span>

<span data-ttu-id="897d4-136">SignalR предоставляет атрибут [Authorize,](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) чтобы указать, какие пользователи имеют доступ к концентратору или методу.</span><span class="sxs-lookup"><span data-stu-id="897d4-136">SignalR provides the [Authorize](https://msdn.microsoft.com/library/microsoft.aspnet.signalr.authorizeattribute(v=vs.111).aspx) attribute to specify which users have access to a hub or method.</span></span> <span data-ttu-id="897d4-137">Атрибут Authorize применяется либо к концентратору, либо к определенным методам в концентраторе.</span><span class="sxs-lookup"><span data-stu-id="897d4-137">You apply the Authorize attribute to either a hub or particular methods in a hub.</span></span> <span data-ttu-id="897d4-138">Без атрибута Authorize все общедоступные методы концентратора доступны клиенту, подключенному к концентратору.</span><span class="sxs-lookup"><span data-stu-id="897d4-138">Without the Authorize attribute, all public methods on the hub are available to a client that is connected to the hub.</span></span> <span data-ttu-id="897d4-139">Для получения дополнительной информации о концентрах, [см. Аутентификация и авторизация для концентраторов SignalR](hub-authorization.md).</span><span class="sxs-lookup"><span data-stu-id="897d4-139">For more information about hubs, see [Authentication and Authorization for SignalR Hubs](hub-authorization.md).</span></span>

<span data-ttu-id="897d4-140">Атрибут применяется `Authorize` к концентратам, но не к постоянным соединениям.</span><span class="sxs-lookup"><span data-stu-id="897d4-140">You apply the `Authorize` attribute to hubs, but not persistent connections.</span></span> <span data-ttu-id="897d4-141">Для обеспечения соблюдения `PersistentConnection` правил авторизации `AuthorizeRequest` при использовании метода необходимо переопределить метод.</span><span class="sxs-lookup"><span data-stu-id="897d4-141">To enforce authorization rules when using a `PersistentConnection` you must override the `AuthorizeRequest` method.</span></span> <span data-ttu-id="897d4-142">Для получения дополнительной информации [Authentication and Authorization for SignalR Persistent Connections](persistent-connection-authorization.md)о постоянных соединениях см.</span><span class="sxs-lookup"><span data-stu-id="897d4-142">For more information about persistent connections, see [Authentication and Authorization for SignalR Persistent Connections](persistent-connection-authorization.md).</span></span>

<a id="connectiontoken"></a>

### <a name="connection-token"></a><span data-ttu-id="897d4-143">Токен подключения</span><span class="sxs-lookup"><span data-stu-id="897d4-143">Connection token</span></span>

<span data-ttu-id="897d4-144">SignalR снижает риск выполнения вредоносных команд, подтверждая личность отправителя.</span><span class="sxs-lookup"><span data-stu-id="897d4-144">SignalR mitigates the risk of executing malicious commands by validating the identity of the sender.</span></span> <span data-ttu-id="897d4-145">Для каждого запроса клиент и сервер передают маркер соединения, содержащий идентификатор соединения и имя пользователя для аутентифицированных пользователей.</span><span class="sxs-lookup"><span data-stu-id="897d4-145">For each request, the client and server pass a connection token which contains the connection id and username for authenticated users.</span></span> <span data-ttu-id="897d4-146">Идентификатор соединения однозначно идентифицирует каждого подключенного клиента.</span><span class="sxs-lookup"><span data-stu-id="897d4-146">The connection id uniquely identifies each connected client.</span></span> <span data-ttu-id="897d4-147">Сервер случайным образом генерирует идентификатор соединения при создании нового соединения и сохраняет этот идентификатор в течение всего периода соединения.</span><span class="sxs-lookup"><span data-stu-id="897d4-147">The server randomly generates the connection id when a new connection is created, and persists that id for the duration of the connection.</span></span> <span data-ttu-id="897d4-148">Механизм проверки подлинности для веб-приложения предоставляет имя пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-148">The authentication mechanism for the web application provides the username.</span></span> <span data-ttu-id="897d4-149">SignalR использует шифрование и цифровую подпись для защиты токена соединения.</span><span class="sxs-lookup"><span data-stu-id="897d4-149">SignalR uses encryption and a digital signature to protect the connection token.</span></span>

![](introduction-to-security/_static/image2.png)

<span data-ttu-id="897d4-150">Для каждого запроса сервер проверяет содержимое маркера, чтобы убедиться, что запрос исходит от указанного пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-150">For each request, the server validates the contents of the token to ensure that the request is coming from the specified user.</span></span> <span data-ttu-id="897d4-151">Имя пользователя должно соответствовать идентификатору соединения. Проверка идентификатора соединения и имени пользователя, SignalR предотвращает злоумышленника от легковыдающейся идентификации другого пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-151">The username must correspond to the connection id. By validating both the connection id and the username, SignalR prevents a malicious user from easily impersonating another user.</span></span> <span data-ttu-id="897d4-152">Если сервер не может проверить маркер соединения, запрос завершается сбой.</span><span class="sxs-lookup"><span data-stu-id="897d4-152">If the server cannot validate the connection token, the request fails.</span></span>

![](introduction-to-security/_static/image4.png)

<span data-ttu-id="897d4-153">Поскольку идентификатор соединения является частью процесса проверки, не следует раскрывать идентификатор соединения одного пользователя другим пользователям или хранить значение на клиенте, например в файле cookie.</span><span class="sxs-lookup"><span data-stu-id="897d4-153">Because the connection id is part of the verification process, you should not reveal one user's connection id to other users or store the value on the client, such as in a cookie.</span></span>

#### <a name="connection-tokens-vs-other-token-types"></a><span data-ttu-id="897d4-154">Токены соединения по сравнению с другими типами токенов</span><span class="sxs-lookup"><span data-stu-id="897d4-154">Connection tokens vs. other token types</span></span>

<span data-ttu-id="897d4-155">Токены соединения иногда помечаются средствами безопасности, поскольку они кажутся маркерами сеанса или маркерами проверки подлинности, что создает риск при их воздействии.</span><span class="sxs-lookup"><span data-stu-id="897d4-155">Connection tokens are occasionally flagged by security tools because they appear to be session tokens or authentication tokens, which poses a risk if exposed.</span></span>

<span data-ttu-id="897d4-156">Токен соединения SignalR не является маркером проверки подлинности.</span><span class="sxs-lookup"><span data-stu-id="897d4-156">SignalR's connection token isn't an authentication token.</span></span> <span data-ttu-id="897d4-157">Он используется для подтверждения того, что пользователь, делающих этот запрос, является тем же самым, что создал соединение.</span><span class="sxs-lookup"><span data-stu-id="897d4-157">It is used to confirm that the user making this request is the same one that created the connection.</span></span> <span data-ttu-id="897d4-158">Токен соединения необходим, поскольку ASP.NET SignalR позволяет соединениям перемещаться между серверами.</span><span class="sxs-lookup"><span data-stu-id="897d4-158">The connection token is necessary because ASP.NET SignalR allows connections to move between servers.</span></span> <span data-ttu-id="897d4-159">Токен связывает соединение с определенным пользователем, но не утверждает личность пользователя, делающего запрос.</span><span class="sxs-lookup"><span data-stu-id="897d4-159">The token associates the connection with a particular user but doesn't assert the identity of the user making the request.</span></span> <span data-ttu-id="897d4-160">Для того чтобы запрос SignalR был надлежащим образом проверен, он должен иметь другой маркер, утверждающий личность пользователя, например маркер или маркер носителя.</span><span class="sxs-lookup"><span data-stu-id="897d4-160">For a SignalR request to be properly authenticated, it must have some other token that asserts the identity of the user, such as a cookie or bearer token.</span></span> <span data-ttu-id="897d4-161">Однако сам маркер соединения не утверждает, что запрос был сделан этим пользователем, только то, что идентификатор соединения, содержащийся в маркере, связан с этим пользователем.</span><span class="sxs-lookup"><span data-stu-id="897d4-161">However, the connection token itself makes no claim that the request was made by that user, only that the connection ID contained within the token is associated with that user.</span></span>

<span data-ttu-id="897d4-162">Поскольку маркер соединения не предоставляет собственной претензии по проверке подлинности, он не считается маркером "сессия" или "аутентификация".</span><span class="sxs-lookup"><span data-stu-id="897d4-162">Since the connection token provides no authentication claim of its own, it isn't considered a "session" or "authentication" token.</span></span> <span data-ttu-id="897d4-163">Принимая токен соединения данного пользователя и восхваивая его в запросе, проверенном как другой пользователь (или непроверенный запрос), не удастся, потому что идентификация пользователя запроса и идентификатор, хранящийся в маркере, не совпадают.</span><span class="sxs-lookup"><span data-stu-id="897d4-163">Taking a given user's connection token and replaying it in a request authenticated as a different user (or an unauthenticated request) will fail, because the user identity of the request and the identity stored in the token won't match.</span></span>

<a id="rejoingroup"></a>

### <a name="rejoining-groups-when-reconnecting"></a><span data-ttu-id="897d4-164">Воссоединение групп при повторном подключении</span><span class="sxs-lookup"><span data-stu-id="897d4-164">Rejoining groups when reconnecting</span></span>

<span data-ttu-id="897d4-165">По умолчанию приложение SignalR автоматически переназначит пользователя соответствующим группам при повторном подключении от временного сбоя, например, когда соединение отброшено и восстановлено до отключения соединения. При повторном подключении клиент передает маркер группы, включающий идентификатор соединения и назначенные группы.</span><span class="sxs-lookup"><span data-stu-id="897d4-165">By default, the SignalR application will automatically re-assign a user to the appropriate groups when reconnecting from a temporary disruption, such as when a connection is dropped and re-established before the connection times out. When reconnecting, the client passes a group token that includes the connection id and the assigned groups.</span></span> <span data-ttu-id="897d4-166">Токен группы подписывается в цифровом виде и шифруется.</span><span class="sxs-lookup"><span data-stu-id="897d4-166">The group token is digitally signed and encrypted.</span></span> <span data-ttu-id="897d4-167">Клиент сохраняет тот же идентификатор соединения после повторного подключения; таким образом, идентификатор соединения, передаваемый от повторно подключенного клиента, должен соответствовать предыдущему идентификатору соединения, используемому клиентом.</span><span class="sxs-lookup"><span data-stu-id="897d4-167">The client retains the same connection id after a reconnection; therefore, the connection id passed from the reconnected client must match the previous connection id used by the client.</span></span> <span data-ttu-id="897d4-168">Эта проверка не позволяет злоумышленнику передавать запросы на присоединение несанкционированных групп при повторном подключении.</span><span class="sxs-lookup"><span data-stu-id="897d4-168">This verification prevents a malicious user from passing requests to join unauthorized groups when reconnecting.</span></span>

<span data-ttu-id="897d4-169">Однако важно отметить, что у группы токен не истекает.</span><span class="sxs-lookup"><span data-stu-id="897d4-169">However, it's important to note, that the group token does not expire.</span></span> <span data-ttu-id="897d4-170">Если пользователь принадлежал к группе в прошлом, но был запрещен в этой группе, этот пользователь может имитировать маркер группы, включающий запрещенную группу.</span><span class="sxs-lookup"><span data-stu-id="897d4-170">If a user belonged to a group in the past, but was banned from that group, that user may be able to mimic a group token that includes the prohibited group.</span></span> <span data-ttu-id="897d4-171">Если вам необходимо надежно управлять тем, какие пользователи принадлежат к каким группам, необходимо хранить эти данные на сервере, например, в базе данных.</span><span class="sxs-lookup"><span data-stu-id="897d4-171">If you need to securely manage which users belong to which groups, you need to store that data on the server, such as in a database.</span></span> <span data-ttu-id="897d4-172">Затем добавьте в приложение логику, которая проверяет на сервере, принадлежит ли пользователь к группе.</span><span class="sxs-lookup"><span data-stu-id="897d4-172">Then, add logic to your application that verifies on the server whether a user belongs to a group.</span></span> <span data-ttu-id="897d4-173">Пример подтверждения членства в группе [см.](../guide-to-the-api/working-with-groups.md)</span><span class="sxs-lookup"><span data-stu-id="897d4-173">For an example of verifying group membership, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<span data-ttu-id="897d4-174">Автоматические воссоединение групп применяется только при повторном подключении соединения после временного сбоя.</span><span class="sxs-lookup"><span data-stu-id="897d4-174">Automatically rejoining groups only applies when a connection is reconnected after a temporary disruption.</span></span> <span data-ttu-id="897d4-175">Если пользователь отключается, отобравшись от приложения или перезагружаясь, приложение должно разобраться, как добавить этого пользователя в правильные группы.</span><span class="sxs-lookup"><span data-stu-id="897d4-175">If a user disconnects by navigating away from the application or the application restarts, your application must handle how to add that user to the correct groups.</span></span> <span data-ttu-id="897d4-176">Для получения дополнительной информации [см.](../guide-to-the-api/working-with-groups.md)</span><span class="sxs-lookup"><span data-stu-id="897d4-176">For more information, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="csrf"></a>

## <a name="how-signalr-prevents-cross-site-request-forgery"></a><span data-ttu-id="897d4-177">Как SignalR предотвращает подделку запросов на перекрестные сайты</span><span class="sxs-lookup"><span data-stu-id="897d4-177">How SignalR prevents Cross-Site Request Forgery</span></span>

<span data-ttu-id="897d4-178">Кросс-сайт Запрос подделка (CSRF) является атака, когда вредоносный сайт отправляет запрос на уязвимый сайт, где пользователь в настоящее время войти в систему.</span><span class="sxs-lookup"><span data-stu-id="897d4-178">Cross-Site Request Forgery (CSRF) is an attack where a malicious site sends a request to a vulnerable site where the user is currently logged in.</span></span> <span data-ttu-id="897d4-179">SignalR предотвращает CSRF, делая крайне маловероятным для вредоносного сайта, чтобы создать действительный запрос для вашего приложения SignalR.</span><span class="sxs-lookup"><span data-stu-id="897d4-179">SignalR prevents CSRF by making it extremely unlikely for a malicious site to create a valid request for your SignalR application.</span></span>

### <a name="description-of-csrf-attack"></a><span data-ttu-id="897d4-180">Описание атаки CSRF</span><span class="sxs-lookup"><span data-stu-id="897d4-180">Description of CSRF attack</span></span>

<span data-ttu-id="897d4-181">Вот пример атаки CSRF:</span><span class="sxs-lookup"><span data-stu-id="897d4-181">Here is an example of a CSRF attack:</span></span>

1. <span data-ttu-id="897d4-182">Пользователь входит в www.example.com, используя аутентификацию форм.</span><span class="sxs-lookup"><span data-stu-id="897d4-182">A user logs into www.example.com, using forms authentication.</span></span>
2. <span data-ttu-id="897d4-183">Сервер проверяет подлинность пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-183">The server authenticates the user.</span></span> <span data-ttu-id="897d4-184">Ответ от сервера включает в себя файлы cookie для проверки подлинности.</span><span class="sxs-lookup"><span data-stu-id="897d4-184">The response from the server includes an authentication cookie.</span></span>
3. <span data-ttu-id="897d4-185">Не выходя из системы, пользователь посещает вредоносный веб-сайт.</span><span class="sxs-lookup"><span data-stu-id="897d4-185">Without logging out, the user visits a malicious web site.</span></span> <span data-ttu-id="897d4-186">Этот вредоносный сайт содержит следующую форму HTML:</span><span class="sxs-lookup"><span data-stu-id="897d4-186">This malicious site contains the following HTML form:</span></span>

    [!code-html[Main](introduction-to-security/samples/sample1.html)]

   <span data-ttu-id="897d4-187">Обратите внимание, что форма действия должности на уязвимый сайт, а не на вредоносный сайт.</span><span class="sxs-lookup"><span data-stu-id="897d4-187">Notice that the form action posts to the vulnerable site, not to the malicious site.</span></span> <span data-ttu-id="897d4-188">Это "кросс-сайт" часть CSRF.</span><span class="sxs-lookup"><span data-stu-id="897d4-188">This is the "cross-site" part of CSRF.</span></span>
4. <span data-ttu-id="897d4-189">Пользователь нажимает кнопку отправки.</span><span class="sxs-lookup"><span data-stu-id="897d4-189">The user clicks the submit button.</span></span> <span data-ttu-id="897d4-190">Браузер включает в себя файлы проверки подлинности с запросом.</span><span class="sxs-lookup"><span data-stu-id="897d4-190">The browser includes the authentication cookie with the request.</span></span>
5. <span data-ttu-id="897d4-191">Запрос выполняется на example.com сервере с контекстом проверки подлинности пользователя и может делать все, что разрешено для проверки подлинности пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-191">The request runs on the example.com server with the user's authentication context, and can do anything that an authenticated user is allowed to do.</span></span>

<span data-ttu-id="897d4-192">Хотя этот пример требует, чтобы пользователь нажал кнопку формы, вредоносная страница может так же легко запустить скрипт, который отправляет запрос AJAX в приложение SignalR.</span><span class="sxs-lookup"><span data-stu-id="897d4-192">Although this example requires the user to click the form button, the malicious page could just as easily run a script that sends an AJAX request to your SignalR application.</span></span> <span data-ttu-id="897d4-193">Кроме того, использование SSL не предотвращает атаку CSRF, так как вредоносный сайт может отправить запрос "https://".</span><span class="sxs-lookup"><span data-stu-id="897d4-193">Moreover, using SSL does not prevent a CSRF attack, because the malicious site can send an "https://" request.</span></span>

<span data-ttu-id="897d4-194">Как правило, атаки CSRF возможны против веб-сайтов, которые используют файлы cookie для проверки подлинности, потому что браузеры отправляют все соответствующие файлы cookie на веб-сайт назначения.</span><span class="sxs-lookup"><span data-stu-id="897d4-194">Typically, CSRF attacks are possible against web sites that use cookies for authentication, because browsers send all relevant cookies to the destination web site.</span></span> <span data-ttu-id="897d4-195">Однако атаки CSRF не ограничиваются использованием файлов cookie.</span><span class="sxs-lookup"><span data-stu-id="897d4-195">However, CSRF attacks are not limited to exploiting cookies.</span></span> <span data-ttu-id="897d4-196">Например, уязвимость также является достоверной проверкой элементарной и дайджестной проверки.</span><span class="sxs-lookup"><span data-stu-id="897d4-196">For example, Basic and Digest authentication are also vulnerable.</span></span> <span data-ttu-id="897d4-197">После того, как пользователь входит в систему с помощью проверки подлинности Basic или Digest, браузер автоматически отправляет учетные данные до окончания сеанса.</span><span class="sxs-lookup"><span data-stu-id="897d4-197">After a user logs in with Basic or Digest authentication, the browser automatically sends the credentials until the session ends.</span></span>

### <a name="csrf-mitigations-taken-by-signalr"></a><span data-ttu-id="897d4-198">Смягчения csRF, принятые SignalR</span><span class="sxs-lookup"><span data-stu-id="897d4-198">CSRF mitigations taken by SignalR</span></span>

<span data-ttu-id="897d4-199">SignalR предпринимает следующие шаги, чтобы предотвратить создание вредоносного сайта действительных запросов в ваше приложение.</span><span class="sxs-lookup"><span data-stu-id="897d4-199">SignalR takes the following steps to prevent a malicious site from creating valid requests to your application.</span></span> <span data-ttu-id="897d4-200">SignalR принимает эти меры по умолчанию, вам не нужно предпринимать никаких действий в коде.</span><span class="sxs-lookup"><span data-stu-id="897d4-200">SignalR takes these steps by default, you do not need to take any action in your code.</span></span>

- <span data-ttu-id="897d4-201">**Отключить запросы на перекрестный домен** SignalR отстраняет от перекрестных запросов домена, чтобы пользователи не вызывали конечную точку SignalR из внешнего домена.</span><span class="sxs-lookup"><span data-stu-id="897d4-201">**Disable cross domain requests** SignalR disables cross domain requests to prevent users from calling a SignalR endpoint from an external domain.</span></span> <span data-ttu-id="897d4-202">SignalR считает любой запрос из внешнего домена недействительным и блокирует запрос.</span><span class="sxs-lookup"><span data-stu-id="897d4-202">SignalR considers any request from an external domain to be invalid and blocks the request.</span></span> <span data-ttu-id="897d4-203">Мы рекомендуем вам сохранить это поведение по умолчанию; в противном случае вредоносный сайт может обмануть пользователей в отправке команд на ваш сайт.</span><span class="sxs-lookup"><span data-stu-id="897d4-203">We recommend that you keep this default behavior; otherwise, a malicious site could trick users into sending commands to your site.</span></span> <span data-ttu-id="897d4-204">Если вам нужно использовать перекрестные запросы домена, [см. Как установить соединение кросс-домена](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span><span class="sxs-lookup"><span data-stu-id="897d4-204">If you need to use cross domain requests, see     [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain) .</span></span>
- <span data-ttu-id="897d4-205">**Передайте токен соединения в строке запроса, а не cookie** SignalR передает маркер соединения как значение строки запроса, а не как файл cookie.</span><span class="sxs-lookup"><span data-stu-id="897d4-205">**Pass connection token in query string, not cookie** SignalR passes the connection token as a query string value, instead of as a cookie.</span></span> <span data-ttu-id="897d4-206">Хранение маркера соединения в файле cookie небезопасно, поскольку браузер может непреднамеренно перенаправить маркер соединения при обнаружении вредоносного кода.</span><span class="sxs-lookup"><span data-stu-id="897d4-206">Storing the connection token in a cookie is unsafe because the browser can inadvertently forward the connection token when malicious code is encountered.</span></span> <span data-ttu-id="897d4-207">Кроме того, передача маркера соединения в строке запроса предотвращает сохранение маркера соединения за пределами текущего соединения.</span><span class="sxs-lookup"><span data-stu-id="897d4-207">Also, passing the connection token in the query string prevents the connection token from persisting beyond the current connection.</span></span> <span data-ttu-id="897d4-208">Таким образом, вредоносный пользователь не может сделать запрос под учетными данными проверки подлинности другого пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-208">Therefore, a malicious user cannot make a request under another user's authentication credentials.</span></span>
- <span data-ttu-id="897d4-209">**Проверка токена соединения** Как описано в разделе [маркеров соединения,](#connectiontoken) сервер знает, какой идентификатор соединения связан с каждым проверенным пользователем.</span><span class="sxs-lookup"><span data-stu-id="897d4-209">**Verify connection token** As described in the     [Connection token](#connectiontoken) section, the server knows which connection id is associated with each authenticated user.</span></span> <span data-ttu-id="897d4-210">Сервер не обрабатывает запрос от идентификатора соединения, который не соответствует имени пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-210">The server does not process any request from a connection id that does not match the user name.</span></span> <span data-ttu-id="897d4-211">Маловероятно, что вредоносный пользователь может угадать действительный запрос, потому что злоумышленник должен знать имя пользователя и текущий случайный идентификатор соединения. Этот идентификатор соединения становится недействительным, как только соединение будет прекращено.</span><span class="sxs-lookup"><span data-stu-id="897d4-211">It is unlikely a malicious user could guess a valid request because the malicious user would have to know the user name and the current randomly-generated connection id. That connection id becomes invalid as soon as the connection is ended.</span></span> <span data-ttu-id="897d4-212">Анонимные пользователи не должны иметь доступ к какой-либо конфиденциальной информации.</span><span class="sxs-lookup"><span data-stu-id="897d4-212">Anonymous users should not have access to any sensitive information.</span></span>

<a id="recommendations"></a>

## <a name="signalr-security-recommendations"></a><span data-ttu-id="897d4-213">Рекомендации по безопасности SignalR</span><span class="sxs-lookup"><span data-stu-id="897d4-213">SignalR Security Recommendations</span></span>

<a id="ssl"></a>

### <a name="secure-socket-layers-ssl-protocol"></a><span data-ttu-id="897d4-214">Протокол «Безопасные слои розетки» (SSL)</span><span class="sxs-lookup"><span data-stu-id="897d4-214">Secure Socket Layers (SSL) protocol</span></span>

<span data-ttu-id="897d4-215">Протокол SSL использует шифрование для защиты передачи данных между клиентом и сервером.</span><span class="sxs-lookup"><span data-stu-id="897d4-215">The SSL protocol uses encryption to secure the transport of data between a client and server.</span></span> <span data-ttu-id="897d4-216">Если приложение SignalR передает конфиденциальную информацию между клиентом и сервером, используйте SSL для транспортировки.</span><span class="sxs-lookup"><span data-stu-id="897d4-216">If your SignalR application transmits sensitive information between the client and server, use SSL for the transport.</span></span> <span data-ttu-id="897d4-217">Для получения дополнительной информации о настройке SSL, [см. Как настроить SSL на IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span><span class="sxs-lookup"><span data-stu-id="897d4-217">For more information about setting up SSL, see [How to set up SSL on IIS 7](https://www.iis.net/learn/manage/configuring-security/how-to-set-up-ssl-on-iis).</span></span>

<a id="groupsecurity"></a>

### <a name="do-not-use-groups-as-a-security-mechanism"></a><span data-ttu-id="897d4-218">Не используйте группы в качестве механизма безопасности</span><span class="sxs-lookup"><span data-stu-id="897d4-218">Do not use groups as a security mechanism</span></span>

<span data-ttu-id="897d4-219">Группы являются удобным способом сбора связанных пользователей, но они не являются безопасным механизмом для ограничения доступа к конфиденциальной информации.</span><span class="sxs-lookup"><span data-stu-id="897d4-219">Groups are a convenient way of collecting related users, but they are not a secure mechanism for limiting access to sensitive information.</span></span> <span data-ttu-id="897d4-220">Это особенно верно, когда пользователи могут автоматически воссоединяться с группами во время повторного подключения.</span><span class="sxs-lookup"><span data-stu-id="897d4-220">This is especially true when users can automatically rejoin groups during a reconnect.</span></span> <span data-ttu-id="897d4-221">Вместо этого рассмотрите возможность добавления привилегированных пользователей к роли и ограничения доступа к методу концентратора только для членов этой роли.</span><span class="sxs-lookup"><span data-stu-id="897d4-221">Instead, consider adding privileged users to a role and limiting access to a hub method to only members of that role.</span></span> <span data-ttu-id="897d4-222">Пример ограничения доступа на основе роли [см.](hub-authorization.md)</span><span class="sxs-lookup"><span data-stu-id="897d4-222">For an example of restricting access based on a role, see [Authentication and Authorization for SignalR Hubs](hub-authorization.md).</span></span> <span data-ttu-id="897d4-223">Например, проверка доступа пользователей к группам при повторном подключении [см.](../guide-to-the-api/working-with-groups.md)</span><span class="sxs-lookup"><span data-stu-id="897d4-223">For an example of checking user access to groups when reconnecting, see [Working with groups](../guide-to-the-api/working-with-groups.md).</span></span>

<a id="input"></a>

### <a name="safely-handling-input-from-clients"></a><span data-ttu-id="897d4-224">Безопасная обработка входных данных от клиентов</span><span class="sxs-lookup"><span data-stu-id="897d4-224">Safely handling input from clients</span></span>

<span data-ttu-id="897d4-225">Чтобы гарантировать, что вредоносный пользователь не отправляет скрипт другим пользователям, необходимо кодировать все входные данные от клиентов, предназначенные для трансляции другим клиентам.</span><span class="sxs-lookup"><span data-stu-id="897d4-225">To ensure that a malicious user does not send script to other users, you must encode all input from clients that is intended for broadcast to other clients.</span></span> <span data-ttu-id="897d4-226">Вы должны кодировать сообщения на принимающих клиентах, а не на сервере, потому что ваше приложение SignalR может иметь много различных типов клиентов.</span><span class="sxs-lookup"><span data-stu-id="897d4-226">You should encode messages on the receiving clients rather than the server, because your SignalR application may have many different types of clients.</span></span> <span data-ttu-id="897d4-227">Таким образом, HTML-кодирование работает для веб-клиента, но не для других типов клиентов.</span><span class="sxs-lookup"><span data-stu-id="897d4-227">Therefore, HTML-encoding works for a web client, but not for other types of clients.</span></span> <span data-ttu-id="897d4-228">Например, метод веб-клиента для отображения сообщения чата безопасно `html()` обрабатывает имя пользователя и сообщение, вызывая функцию.</span><span class="sxs-lookup"><span data-stu-id="897d4-228">For example, a web client method to display a chat message would safely handle the user name and message by calling the `html()` function.</span></span>

[!code-html[Main](introduction-to-security/samples/sample2.html?highlight=3-4)]

<a id="reconcile"></a>

### <a name="reconciling-a-change-in-user-status-with-an-active-connection"></a><span data-ttu-id="897d4-229">Согласование изменения статуса пользователя с активным подключением</span><span class="sxs-lookup"><span data-stu-id="897d4-229">Reconciling a change in user status with an active connection</span></span>

<span data-ttu-id="897d4-230">Если статус аутентификации пользователя изменяется при наличии активного соединения, пользователь получает ошибку, в ходе которого говорится: "Идентификация пользователя не может изменяться во время активного подключения SignalR".</span><span class="sxs-lookup"><span data-stu-id="897d4-230">If a user's authentication status changes while an active connection exists, the user will receive an error that states, "The user identity cannot change during an active SignalR connection."</span></span> <span data-ttu-id="897d4-231">В этом случае приложение должно повторно подключиться к серверу, чтобы убедиться, что идентификатор соединения и имя пользователя скоординированы.</span><span class="sxs-lookup"><span data-stu-id="897d4-231">In that case, your application should re-connect to the server to make sure the connection id and username are coordinated.</span></span> <span data-ttu-id="897d4-232">Например, если приложение позволяет пользователю выйти из системы, пока существует активное соединение, имя пользователя для соединения больше не будет совпадать с именем, которое передается для следующего запроса.</span><span class="sxs-lookup"><span data-stu-id="897d4-232">For example, if your application allows the user to log out while an active connection exists, the username for the connection will no longer match the name that is passed in for the next request.</span></span> <span data-ttu-id="897d4-233">Вы хотите, чтобы остановить соединение, прежде чем пользователь выходит из системы, а затем перезапустить его.</span><span class="sxs-lookup"><span data-stu-id="897d4-233">You will want to stop the connection before the user logs out, and then restart it.</span></span>

<span data-ttu-id="897d4-234">Однако важно отметить, что большинству приложений не нужно будет вручную останавливать и запускать соединение.</span><span class="sxs-lookup"><span data-stu-id="897d4-234">However, it is important to note that most applications will not need to manually stop and start the connection.</span></span> <span data-ttu-id="897d4-235">Если приложение перенаправляет пользователей на отдельную страницу после выхода из системы, например поведение по умолчанию в приложении Web Forms или MVC, или обновляет текущую страницу после выхода из системы, активное соединение автоматически отключается и не требует каких-либо дополнительных действий.</span><span class="sxs-lookup"><span data-stu-id="897d4-235">If your application redirects users to a separate page after logging out, such as the default behavior in a Web Forms application or MVC application, or refreshes the current page after logging out, the active connection is automatically disconnected and does not require any additional action.</span></span>

<span data-ttu-id="897d4-236">В следующем примере показано, как остановить и запустить соединение при изменении статуса пользователя.</span><span class="sxs-lookup"><span data-stu-id="897d4-236">The following example shows how to stop and start a connection when the user status has changed.</span></span>

[!code-html[Main](introduction-to-security/samples/sample3.html)]

<span data-ttu-id="897d4-237">Или статус аутентификации пользователя может измениться, если ваш сайт использует скользящий срок действия с формами аутентификации, и нет никакой активности, чтобы сохранить файлы проверки подлинности действительными.</span><span class="sxs-lookup"><span data-stu-id="897d4-237">Or, the user's authentication status may change if your site uses sliding expiration with Forms Authentication, and there is no activity to keep the authentication cookie valid.</span></span> <span data-ttu-id="897d4-238">В этом случае пользователь будет выходить из системы, и имя пользователя больше не будет совпадать с именем пользователя в маркере соединения.</span><span class="sxs-lookup"><span data-stu-id="897d4-238">In that case, the user will be logged out and the user name will no longer match the user name in the connection token.</span></span> <span data-ttu-id="897d4-239">Эту проблему можно устранить, добавив некоторый скрипт, который периодически запрашивает ресурс на веб-сервере, чтобы сохранить проверку подлинности.</span><span class="sxs-lookup"><span data-stu-id="897d4-239">You can fix this problem by adding some script that periodically requests a resource on the web server to keep the authentication cookie valid.</span></span> <span data-ttu-id="897d4-240">В следующем примере показано, как запрашивать ресурс каждые 30 минут.</span><span class="sxs-lookup"><span data-stu-id="897d4-240">The following example shows how to request a resource every 30 minutes.</span></span>

[!code-javascript[Main](introduction-to-security/samples/sample4.js)]

<a id="autogen"></a>

### <a name="automatically-generated-javascript-proxy-files"></a><span data-ttu-id="897d4-241">Автоматически генерируемые прокси-файлы JavaScript</span><span class="sxs-lookup"><span data-stu-id="897d4-241">Automatically generated JavaScript proxy files</span></span>

<span data-ttu-id="897d4-242">Если вы не хотите включать все концентраторы и методы в прокси-файл JavaScript для каждого пользователя, вы можете отключить автоматическое генерирование файла.</span><span class="sxs-lookup"><span data-stu-id="897d4-242">If you do not want to include all of the hubs and methods in the JavaScript proxy file for each user, you can disable the automatic generation of the file.</span></span> <span data-ttu-id="897d4-243">Вы можете выбрать этот вариант, если у вас есть несколько концентраторов и методов, но не хотите, чтобы каждый пользователь был в курсе всех методов.</span><span class="sxs-lookup"><span data-stu-id="897d4-243">You might choose this option if you have multiple hubs and methods, but do not want every user to be aware of all of the methods.</span></span> <span data-ttu-id="897d4-244">Вы отключаете автоматическое поколение, установив **EnableJavaScriptProxies** на **ложное.**</span><span class="sxs-lookup"><span data-stu-id="897d4-244">You disable automatic generation by setting **EnableJavaScriptProxies** to **false**.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample5.cs)]

<span data-ttu-id="897d4-245">Для получения дополнительной информации о прокси-файлах JavaScript [см.](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy)</span><span class="sxs-lookup"><span data-stu-id="897d4-245">For more information about the JavaScript proxy files, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span> <a id="exceptions"></a>

### <a name="exceptions"></a><span data-ttu-id="897d4-246">Исключения</span><span class="sxs-lookup"><span data-stu-id="897d4-246">Exceptions</span></span>

<span data-ttu-id="897d4-247">Следует избегать передачи объектов исключений клиентам, поскольку объекты могут передавать конфиденциальную информацию клиентам.</span><span class="sxs-lookup"><span data-stu-id="897d4-247">You should avoid passing exception objects to clients because the objects may expose sensitive information to the clients.</span></span> <span data-ttu-id="897d4-248">Вместо этого позвоните в клиент, который отображает соответствующее сообщение об ошибке.</span><span class="sxs-lookup"><span data-stu-id="897d4-248">Instead, call a method on the client that displays the relevant error message.</span></span>

[!code-csharp[Main](introduction-to-security/samples/sample6.cs)]
