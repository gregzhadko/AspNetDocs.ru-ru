---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Представляем ASP.NET веб-страниц - Основы формы HTML Документы Майкрософт
author: Rick-Anderson
description: В этом уроке показаны основы создания формы ввода и обработки ввода пользователя при использовании ASP.NET web Pages (Razor). И теперь, когда вы ...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 04/06/2020
ms.locfileid: "80675925"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="a7de6-104">Представляем ASP.NET веб-страниц - Основы формы HTML</span><span class="sxs-lookup"><span data-stu-id="a7de6-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="a7de6-105">; автор — [Том ФитцМакен (Tom FitzMacken)](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="a7de6-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="a7de6-106">В этом уроке показаны основы создания формы ввода и обработки ввода пользователя при использовании ASP.NET web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="a7de6-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="a7de6-107">И теперь, когда у вас есть база данных, вы будете использовать свои навыки формы, чтобы позволить пользователям находить конкретные фильмы в базе данных.</span><span class="sxs-lookup"><span data-stu-id="a7de6-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="a7de6-108">Он предполагает, что вы завершили серию через [Введение в отображение данных с помощью ASP.NET веб-страниц](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="a7de6-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="a7de6-109">Из этого руководства вы узнаете, как выполнять такие задачи:</span><span class="sxs-lookup"><span data-stu-id="a7de6-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="a7de6-110">Как создать форму с помощью стандартных элементов HTML.</span><span class="sxs-lookup"><span data-stu-id="a7de6-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="a7de6-111">Как прочитать вход пользователя в форме.</span><span class="sxs-lookup"><span data-stu-id="a7de6-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="a7de6-112">Как создать запрос S'L, который выборочно получает данные с помощью поискового термина, который предоставляет пользователь.</span><span class="sxs-lookup"><span data-stu-id="a7de6-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="a7de6-113">Как иметь поля на странице "помните", что пользователь ввел.</span><span class="sxs-lookup"><span data-stu-id="a7de6-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="a7de6-114">Обсуждались особенности/технологии:</span><span class="sxs-lookup"><span data-stu-id="a7de6-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="a7de6-115">Объект `Request`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-115">The `Request` object.</span></span>
> - <span data-ttu-id="a7de6-116">`Where` Оговорка О СЗЛ.</span><span class="sxs-lookup"><span data-stu-id="a7de6-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="a7de6-117">Что вы создадите</span><span class="sxs-lookup"><span data-stu-id="a7de6-117">What You'll Build</span></span>

<span data-ttu-id="a7de6-118">В предыдущем уроке вы создали базу данных, добавили `WebGrid` в нее данные, а затем использовали помощника для отображения данных.</span><span class="sxs-lookup"><span data-stu-id="a7de6-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="a7de6-119">В этом учебнике вы добавите поле поиска, которое позволяет находить фильмы определенного жанра или название которого содержит любое слово, которое вы вводите.</span><span class="sxs-lookup"><span data-stu-id="a7de6-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="a7de6-120">(Например, вы сможете найти все фильмы, жанр которых "Действие" или название которого содержит "Гарри" или "Приключение".)</span><span class="sxs-lookup"><span data-stu-id="a7de6-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="a7de6-121">Когда вы закончите с этим учебником, вы будете иметь страницу, как эта:</span><span class="sxs-lookup"><span data-stu-id="a7de6-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Страница фильмов с поиском жанра и заголовка](form-basics/_static/image1.png)

<span data-ttu-id="a7de6-123">Часть страницы является такой же, как &mdash; в последнем учебнике сетки.</span><span class="sxs-lookup"><span data-stu-id="a7de6-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="a7de6-124">Разница будет в том, что сетка будет показывать только фильмы, которые вы искали.</span><span class="sxs-lookup"><span data-stu-id="a7de6-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="a7de6-125">О формах HTML</span><span class="sxs-lookup"><span data-stu-id="a7de6-125">About HTML Forms</span></span>

<span data-ttu-id="a7de6-126">(Если у вас есть опыт создания HTML-форм `GET` `POST`и с разницей между и, вы можете пропустить этот раздел.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="a7de6-127">Форма имеет текстовые &mdash; ящики ввода пользователей, кнопки, радиокнопки, флажки, списки выпадающих и так далее.</span><span class="sxs-lookup"><span data-stu-id="a7de6-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="a7de6-128">Пользователи заполняют эти элементы управления или делают выбор, а затем отправляют форму, нажав на кнопку.</span><span class="sxs-lookup"><span data-stu-id="a7de6-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="a7de6-129">Основной синтаксис HTML формы иллюстрируется этим примером:</span><span class="sxs-lookup"><span data-stu-id="a7de6-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="a7de6-130">Когда эта разметка работает на странице, она создает простую форму, которая выглядит как эта иллюстрация:</span><span class="sxs-lookup"><span data-stu-id="a7de6-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Базовая форма HTML, отображаемый в браузере](form-basics/_static/image2.png)

<span data-ttu-id="a7de6-132">Элемент `<form>` примыкает к элементам HTML, которые должны быть представлены.</span><span class="sxs-lookup"><span data-stu-id="a7de6-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="a7de6-133">(Легко сделать ошибку – добавить элементы на страницу, а затем забыть поместить их в `<form>` элемент.</span><span class="sxs-lookup"><span data-stu-id="a7de6-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="a7de6-134">В этом случае ничего не представлено.) Атрибут `method` сообщает браузеру, как отправить пользовательский ввод.</span><span class="sxs-lookup"><span data-stu-id="a7de6-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="a7de6-135">Вы устанавливаете `post` это, если вы выполняете обновление `get` на сервере или если вы просто извлечения данных с сервера.</span><span class="sxs-lookup"><span data-stu-id="a7de6-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="a7de6-136">**GET, POST и HTTP Глагол безопасности**</span><span class="sxs-lookup"><span data-stu-id="a7de6-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="a7de6-137">HTTP, протокол, который браузеры и серверы используют для обмена информацией, удивительно прост в своих основных операциях.</span><span class="sxs-lookup"><span data-stu-id="a7de6-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="a7de6-138">Браузеры используют только несколько глаголов для запросов на серверы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="a7de6-139">Когда вы пишете код для Интернета, полезно понять эти глаголы и как браузер и сервер используют их.</span><span class="sxs-lookup"><span data-stu-id="a7de6-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="a7de6-140">Далеко и далеко наиболее часто используемые глаголы являются следующие:</span><span class="sxs-lookup"><span data-stu-id="a7de6-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="a7de6-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-141">`GET`.</span></span> <span data-ttu-id="a7de6-142">Браузер использует этот глагол, чтобы получить что-то с сервера.</span><span class="sxs-lookup"><span data-stu-id="a7de6-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="a7de6-143">Например, при вводе URL-адреса в браузере `GET` браузер выполняет операцию по запросу нужной страницы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="a7de6-144">Если страница содержит графику, браузер `GET` выполняет дополнительные операции для получения изображений.</span><span class="sxs-lookup"><span data-stu-id="a7de6-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="a7de6-145">Если `GET` операция должна передать информацию серверу, информация передается как часть URL-адреса в строке запроса.</span><span class="sxs-lookup"><span data-stu-id="a7de6-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="a7de6-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-146">`POST`.</span></span> <span data-ttu-id="a7de6-147">Браузер отправляет `POST` запрос для отправки данных для добавления или изменения на сервере.</span><span class="sxs-lookup"><span data-stu-id="a7de6-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="a7de6-148">Например, `POST` глагол используется для создания записей в базе данных или изменения существующих.</span><span class="sxs-lookup"><span data-stu-id="a7de6-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="a7de6-149">Большую часть времени, когда вы заполняете форму и нажимаете кнопку отправки, браузер выполняет операцию. `POST`</span><span class="sxs-lookup"><span data-stu-id="a7de6-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="a7de6-150">В `POST` операции данные, передаваемые на сервер, находится в теле страницы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="a7de6-151">Важное различие между этими `GET` глаголами заключается в том, что операция не должна что-либо `GET` менять на сервере - или, говоря немного более абстрактно, операция не приводит к изменению состояния на сервере.</span><span class="sxs-lookup"><span data-stu-id="a7de6-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="a7de6-152">Вы можете `GET` выполнять операцию на тех же ресурсах столько раз, сколько вам нравится, и эти ресурсы не меняются.</span><span class="sxs-lookup"><span data-stu-id="a7de6-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="a7de6-153">(Операция `GET` часто говорят, что "безопасный", или использовать технический термин, является *идемпотентным*.) В отличие от этого, конечно, `POST` запрос что-то изменяет на сервере каждый раз при выполнении операции.</span><span class="sxs-lookup"><span data-stu-id="a7de6-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="a7de6-154">Два примера помогут проиллюстрировать это различие.</span><span class="sxs-lookup"><span data-stu-id="a7de6-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="a7de6-155">При выполнении поиска с помощью движка, как Bing или Google, вы заполните форму, которая состоит из одного текстового ящика, а затем нажмите кнопку поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="a7de6-156">Браузер выполняет операцию, `GET` при этом значение, вписававеее в поле, передаваемые как часть URL-адреса.</span><span class="sxs-lookup"><span data-stu-id="a7de6-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="a7de6-157">Использование `GET` операции для такого типа формы является прекрасным, потому что поисковая операция не изменяет никаких ресурсов на сервере, он просто получает информацию.</span><span class="sxs-lookup"><span data-stu-id="a7de6-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="a7de6-158">Теперь рассмотрим процесс заказа что-то в Интернете.</span><span class="sxs-lookup"><span data-stu-id="a7de6-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="a7de6-159">Вы заполняете детали заказа, а затем нажмите кнопку отправки.</span><span class="sxs-lookup"><span data-stu-id="a7de6-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="a7de6-160">Эта операция будет `POST` запросом, поскольку операция приведет к изменениям на сервере, таким как новая запись заказа, изменение информации об учетной записи и, возможно, многие другие изменения.</span><span class="sxs-lookup"><span data-stu-id="a7de6-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="a7de6-161">В `GET` отличие от операции, `POST` вы не можете повторить запрос - если вы сделали, каждый раз, когда вы повторно отправили запрос, вы бы создать новый заказ на сервере.</span><span class="sxs-lookup"><span data-stu-id="a7de6-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="a7de6-162">(В подобных случаях веб-сайты часто предупреждают вас не нажимать кнопку отправки более одного раза или отогивают кнопку отправки, чтобы вы случайно не отправили форму повторно.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="a7de6-163">В ходе этого урока вы будете `GET` использовать как `POST` операцию, так и операцию для работы с HTML-формами.</span><span class="sxs-lookup"><span data-stu-id="a7de6-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="a7de6-164">В каждом случае мы объясним, почему используется глагол.</span><span class="sxs-lookup"><span data-stu-id="a7de6-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="a7de6-165">(Чтобы узнать больше о глаголах HTTP, смотрите статью [Определения методов](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) на сайте W3C.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="a7de6-166">Большинство элементов пользовательского `<input>` ввода являются html-элементами.</span><span class="sxs-lookup"><span data-stu-id="a7de6-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="a7de6-167">Они выглядят так, как `<input type="type" name="name">,` *будто тип* указывает на тип пользовательского элемента управления вхданным, который вы хотите.</span><span class="sxs-lookup"><span data-stu-id="a7de6-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="a7de6-168">Эти элементы являются общими:</span><span class="sxs-lookup"><span data-stu-id="a7de6-168">These elements are the common ones:</span></span>

- <span data-ttu-id="a7de6-169">Текстовое окно:`<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="a7de6-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="a7de6-170">Флажок:`<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="a7de6-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="a7de6-171">Кнопка радио:`<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="a7de6-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="a7de6-172">Кнопку:`<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="a7de6-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="a7de6-173">Кнопка отправки:`<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="a7de6-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="a7de6-174">Вы также можете `<textarea>` использовать элемент для создания многолинейного текстового ящика и `<select>` элемента для создания списка выпадающих или прокрутки списка.</span><span class="sxs-lookup"><span data-stu-id="a7de6-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="a7de6-175">(Подробнее об элементах [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) формы HTML см.</span><span class="sxs-lookup"><span data-stu-id="a7de6-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="a7de6-176">Атрибут `name` очень важен, потому что имя — как вы получите значение элемента позже, как вы увидите в ближайшее время.</span><span class="sxs-lookup"><span data-stu-id="a7de6-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="a7de6-177">Интересная часть заключается в том, что вы, разработчик страницы, делаете с вводом пользователя.</span><span class="sxs-lookup"><span data-stu-id="a7de6-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="a7de6-178">С этими элементами нет встроенного поведения.</span><span class="sxs-lookup"><span data-stu-id="a7de6-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="a7de6-179">Вместо этого, вы должны получить значения, которые пользователь ввел или выбрал и сделать что-то с ними.</span><span class="sxs-lookup"><span data-stu-id="a7de6-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="a7de6-180">Это то, что вы узнаете в этом учебнике.</span><span class="sxs-lookup"><span data-stu-id="a7de6-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="a7de6-181">**HTML5 и входные формы**</span><span class="sxs-lookup"><span data-stu-id="a7de6-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="a7de6-182">Как вы знаете, HTML находится в переходном периоде, а последняя версия (HTML5) включает в себя поддержку более интуитивных способов ввода информации пользователями.</span><span class="sxs-lookup"><span data-stu-id="a7de6-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="a7de6-183">Например, в HTML5 вы (разработчик страницы) можете узнать на странице, что вы хотите, чтобы пользователь ввезло дату.</span><span class="sxs-lookup"><span data-stu-id="a7de6-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="a7de6-184">Браузер может автоматически отображать календарь, а не требовать от пользователя вводить дату вручную.</span><span class="sxs-lookup"><span data-stu-id="a7de6-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="a7de6-185">Тем не менее, HTML5 является новым и не поддерживается во всех браузерах еще.</span><span class="sxs-lookup"><span data-stu-id="a7de6-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="a7de6-186">ASP.NET Web Pages поддерживает ввод HTML5 в той мере, в какой это делает браузер пользователя.</span><span class="sxs-lookup"><span data-stu-id="a7de6-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="a7de6-187">Для идеи новых атрибутов `<input>` для элемента в HTML5 см. атрибут [ввода &lt;&gt; HTML](http://www.w3schools.com/html/html_form_input_types.asp) на сайте W3Schools.</span><span class="sxs-lookup"><span data-stu-id="a7de6-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="a7de6-188">Создание формы</span><span class="sxs-lookup"><span data-stu-id="a7de6-188">Creating the Form</span></span>

<span data-ttu-id="a7de6-189">В WebMatrix, в рабочей области **файлов,** откройте страницу *Movies.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="a7de6-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="a7de6-190">После закрытия `</h1>` тега и `<div>` перед `grid.GetHtml` открытием тега вызова, добавить следующую разметку:</span><span class="sxs-lookup"><span data-stu-id="a7de6-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="a7de6-191">Эта разметка создает форму, которая `searchGenre` имеет текстовое поле с именем и кнопкой отправки.</span><span class="sxs-lookup"><span data-stu-id="a7de6-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="a7de6-192">Текстовое поле и кнопка отправки заключены в `<form>` элемент, атрибут которого `method` установлен на. `get`</span><span class="sxs-lookup"><span data-stu-id="a7de6-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="a7de6-193">(Помните, что если вы не поместите текстовый ящик и не отправите кнопку внутри элемента, `<form>` ничего не будет отправлено при нажатии кнопки.) Вы используете `GET` глагол здесь, потому что вы создаете форму, которая не вносят никаких изменений на сервере - это просто приводит к поиску.</span><span class="sxs-lookup"><span data-stu-id="a7de6-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="a7de6-194">(В предыдущем учебнике вы `post` использовали метод, который является, как вы отправляете изменения на сервере.</span><span class="sxs-lookup"><span data-stu-id="a7de6-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="a7de6-195">Вы увидите, что в следующем учебнике снова.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="a7de6-196">Запустите страницу.</span><span class="sxs-lookup"><span data-stu-id="a7de6-196">Run the page.</span></span> <span data-ttu-id="a7de6-197">Хотя вы не определили какое-либо поведение для формы, вы можете увидеть, как она выглядит:</span><span class="sxs-lookup"><span data-stu-id="a7de6-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Страница фильмов с полем поиска для жанра](form-basics/_static/image3.png)

<span data-ttu-id="a7de6-199">Введите значение в текстовое окно, как "Комедия".</span><span class="sxs-lookup"><span data-stu-id="a7de6-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="a7de6-200">Затем нажмите **Поиск жанра**.</span><span class="sxs-lookup"><span data-stu-id="a7de6-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="a7de6-201">Обратите внимание на URL страницы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-201">Take note of the URL of the page.</span></span> <span data-ttu-id="a7de6-202">Поскольку атрибут `<form>` элемента `method` настраивается на `get`вводящее значение, вписанное в строку запроса, в URL-</span><span class="sxs-lookup"><span data-stu-id="a7de6-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="a7de6-203">Значение формы чтения</span><span class="sxs-lookup"><span data-stu-id="a7de6-203">Reading Form Values</span></span>

<span data-ttu-id="a7de6-204">Страница уже содержит некоторый код, который получает данные базы данных и отображает результаты в сетке.</span><span class="sxs-lookup"><span data-stu-id="a7de6-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="a7de6-205">Теперь вам нужно добавить некоторый код, который читает значение текстового поля, так что вы можете запустить запрос S'L, который включает в себя термин поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="a7de6-206">Поскольку вы устанавливаете метод `get`формы для того, чтобы вы могли прочитать значение, которое было введено в текстовое поле, используя код, как следующее:</span><span class="sxs-lookup"><span data-stu-id="a7de6-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="a7de6-207">Объект `Request.QueryString` `QueryString` (свойство `Request` объекта) включает в себя значения элементов, которые `GET` были представлены в рамках операции.</span><span class="sxs-lookup"><span data-stu-id="a7de6-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="a7de6-208">Свойство `Request.QueryString` содержит *коллекцию* (список) значений, которые представлены в форме.</span><span class="sxs-lookup"><span data-stu-id="a7de6-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="a7de6-209">Чтобы получить какое-либо индивидуальное значение, вы указываете имя элемента, который вы хотите.</span><span class="sxs-lookup"><span data-stu-id="a7de6-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="a7de6-210">Вот почему вы должны иметь `name` атрибут `<input>` на`searchTerm`элементе (), который создает текстовое поле.</span><span class="sxs-lookup"><span data-stu-id="a7de6-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="a7de6-211">(Подробнее об `Request` объекте смотрите [боковую панель](#BKMK_TheRequestObject) позже.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="a7de6-212">Это достаточно просто, чтобы прочитать значение текстового окна.</span><span class="sxs-lookup"><span data-stu-id="a7de6-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="a7de6-213">Но если пользователь не введал ничего вообще в текстовом поле, но нажал **Поиск** в любом случае, вы можете игнорировать этот клик, так как нет ничего для поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="a7de6-214">Следующий код является примером, который показывает, как реализовать эти условия.</span><span class="sxs-lookup"><span data-stu-id="a7de6-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="a7de6-215">(Вы не должны добавить этот код еще; вы будете делать это в момент.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="a7de6-216">Тест ломается таким образом:</span><span class="sxs-lookup"><span data-stu-id="a7de6-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="a7de6-217">Получить значение `Request.QueryString["searchGenre"]`, а именно значение, которое `<input>` `searchGenre`было введено в элемент под названием.</span><span class="sxs-lookup"><span data-stu-id="a7de6-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="a7de6-218">Узнайте, пуста ли она `IsEmpty` с помощью метода.</span><span class="sxs-lookup"><span data-stu-id="a7de6-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="a7de6-219">Этот метод является стандартным способом определения того, содержит ли значение что-то (например, элемент формы).</span><span class="sxs-lookup"><span data-stu-id="a7de6-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="a7de6-220">Но на самом деле, вы заботитесь только, если он *не* пуст, поэтому ...</span><span class="sxs-lookup"><span data-stu-id="a7de6-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="a7de6-221">Добавьте `!` оператора перед `IsEmpty` тестом.</span><span class="sxs-lookup"><span data-stu-id="a7de6-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="a7de6-222">(Оператор `!` означает логический НЕ).</span><span class="sxs-lookup"><span data-stu-id="a7de6-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="a7de6-223">На простом английском `if` языке, все условие переводится в следующее: *Если элемент searchGenre формы не пуст, то ...*</span><span class="sxs-lookup"><span data-stu-id="a7de6-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="a7de6-224">Этот блок создает условия для создания запроса, использующего термин поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="a7de6-225">Это будет сделано в следующем разделе.</span><span class="sxs-lookup"><span data-stu-id="a7de6-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="a7de6-226">**Объект запроса**</span><span class="sxs-lookup"><span data-stu-id="a7de6-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="a7de6-227">Объект `Request` содержит всю информацию, которую браузер отправляет в ваше приложение при запросе или отправке страницы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="a7de6-228">Этот объект включает в себя любую информацию, которую пользователь предоставляет, например значения текстового поля или файл для загрузки.</span><span class="sxs-lookup"><span data-stu-id="a7de6-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="a7de6-229">Он также включает в себя все виды дополнительной информации, такие как файлы cookie, значения строки запроса URL (если таковые имеются), путь файла страницы, который работает, тип браузера, который использует пользователь, список языков, которые установлены в браузере, и многое другое.</span><span class="sxs-lookup"><span data-stu-id="a7de6-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="a7de6-230">Объект `Request` представляет собой *набор* (список) значений.</span><span class="sxs-lookup"><span data-stu-id="a7de6-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="a7de6-231">Вы получаете индивидуальную стоимость из коллекции, указывая ее название:</span><span class="sxs-lookup"><span data-stu-id="a7de6-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="a7de6-232">Объект `Request` фактически предоставляет несколько подмножеств.</span><span class="sxs-lookup"><span data-stu-id="a7de6-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="a7de6-233">Пример:</span><span class="sxs-lookup"><span data-stu-id="a7de6-233">For example:</span></span>
> 
> - <span data-ttu-id="a7de6-234">`Request.Form`дает значения из элементов `<form>` внутри представленного элемента, если запрос является запросом. `POST`</span><span class="sxs-lookup"><span data-stu-id="a7de6-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="a7de6-235">`Request.QueryString`дает только значения в строке запроса URL.</span><span class="sxs-lookup"><span data-stu-id="a7de6-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="a7de6-236">(В URL, `http://mysite/myapp/page?searchGenre=action&page=2`как, `?searchGenre=action&page=2` раздел URL строки запроса.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="a7de6-237">`Request.Cookies`коллекция дает вам доступ к файлам cookie, которые браузер отправил.</span><span class="sxs-lookup"><span data-stu-id="a7de6-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="a7de6-238">Чтобы получить значение, которое вы знаете, находится `Request["name"]`в представленной форме, вы можете использовать.</span><span class="sxs-lookup"><span data-stu-id="a7de6-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="a7de6-239">Кроме того, можно использовать более `Request.Form["name"]` конкретные версии (для `POST` запросов) или `Request.QueryString["name"]` (для `GET` запросов).</span><span class="sxs-lookup"><span data-stu-id="a7de6-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="a7de6-240">Конечно, *имя* имя пункта, чтобы получить.</span><span class="sxs-lookup"><span data-stu-id="a7de6-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="a7de6-241">Название предмета, который вы хотите получить, должно быть уникальным в коллекции, которая вы используете.</span><span class="sxs-lookup"><span data-stu-id="a7de6-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="a7de6-242">Вот почему `Request` объект предоставляет подмножества, `Request.Form` `Request.QueryString`как и .</span><span class="sxs-lookup"><span data-stu-id="a7de6-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="a7de6-243">Предположим, что ваша страница `userName` содержит элемент формы `userName`с именем, а *также* содержит файл cookie с именем .</span><span class="sxs-lookup"><span data-stu-id="a7de6-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="a7de6-244">Если вы `Request["userName"]`получаете, это неоднозначно ли вы хотите значение формы или печенье.</span><span class="sxs-lookup"><span data-stu-id="a7de6-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="a7de6-245">Однако, если `Request.Form["userName"]` `Request.Cookie["userName"]`вы получаете или , вы явно о том, какое значение, чтобы получить.</span><span class="sxs-lookup"><span data-stu-id="a7de6-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="a7de6-246">Это хорошая практика, чтобы быть конкретным и `Request` использовать подмножество, что `Request.Form` `Request.QueryString`вы заинтересованы в, как или .</span><span class="sxs-lookup"><span data-stu-id="a7de6-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="a7de6-247">Для простых страниц, которые вы создаете в этом учебнике, это, вероятно, на самом деле не имеет никакого значения.</span><span class="sxs-lookup"><span data-stu-id="a7de6-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="a7de6-248">Однако при создании более сложных страниц, используя явную версию `Request.Form` или `Request.QueryString` может помочь вам избежать проблем, которые могут возникнуть, когда страница содержит форму (или несколько форм), файлы cookie, значения строк запроса и так далее.</span><span class="sxs-lookup"><span data-stu-id="a7de6-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="a7de6-249">Создание запроса с помощью термина поиска</span><span class="sxs-lookup"><span data-stu-id="a7de6-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="a7de6-250">Теперь, когда вы знаете, как получить термин поиска, который ввел пользователь, можно создать запрос, который использует его.</span><span class="sxs-lookup"><span data-stu-id="a7de6-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="a7de6-251">Не забудьте, что для того, чтобы вывести все элементы фильма из базы данных, вы используете запрос S'L, похожий на это утверждение:</span><span class="sxs-lookup"><span data-stu-id="a7de6-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="a7de6-252">Чтобы получить только определенные фильмы, необходимо использовать `Where` запрос, который включает в себя оговорку.</span><span class="sxs-lookup"><span data-stu-id="a7de6-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="a7de6-253">Это положение позволяет установить условие, на котором строки возвращаются запросом.</span><span class="sxs-lookup"><span data-stu-id="a7de6-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="a7de6-254">Ниже приведен пример:</span><span class="sxs-lookup"><span data-stu-id="a7de6-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="a7de6-255">Основной формат `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="a7de6-256">Вы можете использовать различные `>` операторы, `<` кроме просто, `<>` `<=` `=`как (больше, чем), (меньше, чем), (не равна), (меньше, чем или равны), и т.д., в зависимости от того, что вы ищете.</span><span class="sxs-lookup"><span data-stu-id="a7de6-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="a7de6-257">В случае, если вам интересно, заявления &mdash; `SELECT` s'L `Select` не являются `select`чувствительными случае так же, как (или даже).</span><span class="sxs-lookup"><span data-stu-id="a7de6-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="a7de6-258">Тем не менее, люди часто капитализируют `SELECT` ключевые слова в выписке по S'L, например, и, `WHERE`чтобы сделать его легче читать.</span><span class="sxs-lookup"><span data-stu-id="a7de6-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="a7de6-259">Прохождение термина поиска в качестве параметра</span><span class="sxs-lookup"><span data-stu-id="a7de6-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="a7de6-260">Поиск конкретного жанра достаточно`WHERE Genre = 'Action'`легко ( ), но вы хотите, чтобы иметь возможность искать для любого жанра, что пользователь входит.</span><span class="sxs-lookup"><span data-stu-id="a7de6-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="a7de6-261">Для этого вы создаете в качестве запроса S'L, который включает в себя заполнитель для значения для поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="a7de6-262">Это будет выглядеть как эта команда:</span><span class="sxs-lookup"><span data-stu-id="a7de6-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="a7de6-263">Заполнитель `@` символ следует ноль.</span><span class="sxs-lookup"><span data-stu-id="a7de6-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="a7de6-264">Как вы могли догадаться, запрос может содержать несколько заполнителей, и они будут названы `@0`, `@1` `@2`и т.д.</span><span class="sxs-lookup"><span data-stu-id="a7de6-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="a7de6-265">Чтобы настроить запрос и передать его значение, вы используете код, как следующее:</span><span class="sxs-lookup"><span data-stu-id="a7de6-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="a7de6-266">Этот код аналогичен тому, что вы уже сделали для отображения данных в сетке.</span><span class="sxs-lookup"><span data-stu-id="a7de6-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="a7de6-267">Единственные отличия:</span><span class="sxs-lookup"><span data-stu-id="a7de6-267">The only differences are:</span></span>

- <span data-ttu-id="a7de6-268">Запрос содержит заполнитель`WHERE Genre = @0"`().</span><span class="sxs-lookup"><span data-stu-id="a7de6-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="a7de6-269">Запрос вводима в`selectCommand`переменную (); ранее вы передавали запрос непосредственно `db.Query` методу.</span><span class="sxs-lookup"><span data-stu-id="a7de6-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="a7de6-270">При вызове `db.Query` метода вы передаете как запрос, так и значение для использования для заполнителя.</span><span class="sxs-lookup"><span data-stu-id="a7de6-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="a7de6-271">(Если в запросе было несколько заполнителей, вы бы передали их все как отдельные значения методу.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="a7de6-272">Если собрать все эти элементы вместе, вы получите следующий код:</span><span class="sxs-lookup"><span data-stu-id="a7de6-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="a7de6-273">**Важно!**</span><span class="sxs-lookup"><span data-stu-id="a7de6-273">**Important!**</span></span> <span data-ttu-id="a7de6-274">Использование заполнителей (например), `@0`чтобы передать значения команде S'L, чрезвычайно *важно* для безопасности.</span><span class="sxs-lookup"><span data-stu-id="a7de6-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="a7de6-275">То, как вы видите это здесь, с заполнителями для переменных данных, является единственным способом, которым вы должны построить команды S'L.</span><span class="sxs-lookup"><span data-stu-id="a7de6-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="a7de6-276">Никогда не стройте заявление s'L, объединяя (конкатенацию) буквальный текст и значения, которые вы получаете от пользователя.</span><span class="sxs-lookup"><span data-stu-id="a7de6-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="a7de6-277">Совмещение пользовательского ввода в выписку s'L открывает ваш сайт для *атаки на инъекцию S'L,* где вредоносный пользователь отправляет значения на вашу страницу, которая взломает вашу базу данных.</span><span class="sxs-lookup"><span data-stu-id="a7de6-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="a7de6-278">(Вы можете прочитать больше в статье [S'L инъекций](https://msdn.microsoft.com/library/ms161953.aspx) веб-сайт MSDN.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="a7de6-279">Обновление страницы фильмов с помощью кода поиска</span><span class="sxs-lookup"><span data-stu-id="a7de6-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="a7de6-280">Теперь вы можете обновить код в файле *Movies.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="a7de6-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="a7de6-281">Для начала замените код в блоке кода в верхней части страницы этим кодом:</span><span class="sxs-lookup"><span data-stu-id="a7de6-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="a7de6-282">Разница в том, что вы поместили `selectCommand` запрос в переменную, `db.Query` которую перейдите позже.</span><span class="sxs-lookup"><span data-stu-id="a7de6-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="a7de6-283">Ввод оператора S'L в переменную позволяет изменить оператор, что и будет делать для выполнения поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="a7de6-284">Вы также удалили эти две строки, которые вы поставите обратно в позже:</span><span class="sxs-lookup"><span data-stu-id="a7de6-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="a7de6-285">Вы не хотите, чтобы запустить запрос еще (то есть, вызов), `db.Query`и вы `WebGrid` не хотите, чтобы инициализировать помощника еще либо.</span><span class="sxs-lookup"><span data-stu-id="a7de6-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="a7de6-286">Вы будете делать эти вещи после того, как вы поняли, какой s'L выписка должна работать.</span><span class="sxs-lookup"><span data-stu-id="a7de6-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="a7de6-287">После этого переписана блокировка, можно добавить новую логику для обработки поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="a7de6-288">Заполненный код будет выглядеть следующим образом.</span><span class="sxs-lookup"><span data-stu-id="a7de6-288">The completed code will look like the following.</span></span> <span data-ttu-id="a7de6-289">Обновление кода на странице, чтобы он соответствовал этому примеру:</span><span class="sxs-lookup"><span data-stu-id="a7de6-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="a7de6-290">Страница теперь работает следующим образом.</span><span class="sxs-lookup"><span data-stu-id="a7de6-290">The page now works like this.</span></span> <span data-ttu-id="a7de6-291">Каждый раз, когда страница выполняется, `selectCommand` код открывает базу данных, и переменная `Movies` устанавливается в выписку s'L, которая получает все записи из таблицы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="a7de6-292">Код также инициализирует переменную. `searchTerm`</span><span class="sxs-lookup"><span data-stu-id="a7de6-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="a7de6-293">Однако, если текущий запрос `searchGenre` включает значение для `selectCommand` элемента, код устанавливается на другой `Where` запрос, а именно на запрос, который включает положение для поиска жанра.</span><span class="sxs-lookup"><span data-stu-id="a7de6-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="a7de6-294">Он также `searchTerm` устанавливает на все, что было передано для поиска окно (которое может быть ничего).</span><span class="sxs-lookup"><span data-stu-id="a7de6-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="a7de6-295">Независимо от того, в `selectCommand`какой выписке находится S'L, код затем вызывает `db.Query` для выполнения запроса, передавая его на все, что находится в `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="a7de6-296">Если нет ничего `searchTerm`в, это не имеет значения, потому что в этом `selectCommand` случае нет параметра, чтобы передать значение в любом случае.</span><span class="sxs-lookup"><span data-stu-id="a7de6-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="a7de6-297">Наконец, код инициализирует `WebGrid` помощника, используя результаты запроса, как и раньше.</span><span class="sxs-lookup"><span data-stu-id="a7de6-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="a7de6-298">Вы можете видеть, что, разместив оператор s-L и термин поиска в переменные, вы добавили гибкость в код.</span><span class="sxs-lookup"><span data-stu-id="a7de6-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="a7de6-299">Как вы увидите позже в этом учебнике, вы можете использовать эту базовую структуру и продолжать добавлять логику для различных типов поисков.</span><span class="sxs-lookup"><span data-stu-id="a7de6-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="a7de6-300">Тестирование функции поиска по жанру</span><span class="sxs-lookup"><span data-stu-id="a7de6-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="a7de6-301">В WebMatrix запустите страницу *Movies.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="a7de6-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="a7de6-302">Вы видите страницу с текстовым полем для жанра.</span><span class="sxs-lookup"><span data-stu-id="a7de6-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="a7de6-303">Введите жанр, который вы ввели для одной из ваших тестовых записей, а затем нажмите **Поиск.**</span><span class="sxs-lookup"><span data-stu-id="a7de6-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="a7de6-304">На этот раз вы видите список только фильмы, которые соответствуют этому жанру:</span><span class="sxs-lookup"><span data-stu-id="a7de6-304">This time you see a listing of just the movies that match that genre:</span></span>

![Фильмы страницы листинг после поиска жанра 'Комедии'](form-basics/_static/image4.png)

<span data-ttu-id="a7de6-306">Введите другой жанр и поиск снова.</span><span class="sxs-lookup"><span data-stu-id="a7de6-306">Enter a different genre and search again.</span></span> <span data-ttu-id="a7de6-307">Попробуйте войти в жанр, используя все нижние или все буквы верхнего регистра, так что вы можете видеть, что поиск не является случае чувствительным.</span><span class="sxs-lookup"><span data-stu-id="a7de6-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="a7de6-308">"Вспоминая" Что пользователь ввел</span><span class="sxs-lookup"><span data-stu-id="a7de6-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="a7de6-309">Вы могли заметить, что после того, как вы вошли в жанр и нажал **поиск жанра**, вы видели листинг для этого жанра.</span><span class="sxs-lookup"><span data-stu-id="a7de6-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="a7de6-310">Тем не менее, текстовый ящик поиска был пуст, &mdash; другими словами, он не помнит, что вы ввели.</span><span class="sxs-lookup"><span data-stu-id="a7de6-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="a7de6-311">Важно понять, почему такое поведение происходит.</span><span class="sxs-lookup"><span data-stu-id="a7de6-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="a7de6-312">При отправке страницы браузер отправляет запрос на веб-сервер.</span><span class="sxs-lookup"><span data-stu-id="a7de6-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="a7de6-313">Когда ASP.NET получает запрос, он создает совершенно новый экземпляр страницы, запускает код в нем, а затем снова отосвносит страницу в браузер.</span><span class="sxs-lookup"><span data-stu-id="a7de6-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="a7de6-314">В действительности, однако, страница не знает, что вы просто работали с предыдущей версией себя.</span><span class="sxs-lookup"><span data-stu-id="a7de6-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="a7de6-315">Все, что он знает, что он получил запрос, который имел некоторые данные формы в нем.</span><span class="sxs-lookup"><span data-stu-id="a7de6-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="a7de6-316">Каждый раз, &mdash; когда вы запрашиваете страницу, будь то в первый раз или, представив ее, &mdash; вы получаете новую страницу.</span><span class="sxs-lookup"><span data-stu-id="a7de6-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="a7de6-317">Веб-сервер не помнит ваш последний запрос.</span><span class="sxs-lookup"><span data-stu-id="a7de6-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="a7de6-318">Как и ASP.NET, и браузер не работает.</span><span class="sxs-lookup"><span data-stu-id="a7de6-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="a7de6-319">Единственная связь между этими отдельными экземплярами страницы — это любые данные, которые вы передаете между ними.</span><span class="sxs-lookup"><span data-stu-id="a7de6-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="a7de6-320">Например, если вы отправляете страницу, в экземпляр новой страницы можно получить данные формы, отправленные предыдущим экземпляром.</span><span class="sxs-lookup"><span data-stu-id="a7de6-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="a7de6-321">(Еще один способ передачи данных между страницами — использование файлов cookie.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="a7de6-322">Формальный способ описать эту ситуацию заключается в том, чтобы сказать, что веб-страницы *являются абрежными.*</span><span class="sxs-lookup"><span data-stu-id="a7de6-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="a7de6-323">Веб-серверы и сами страницы и элементы страницы не сохраняют никакой информации о предыдущем состоянии страницы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="a7de6-324">Веб был разработан таким образом, потому что поддержание состояния для индивидуальных запросов быстро исчерпает ресурсы веб-серверов, которые часто обрабатывают тысячи, может быть, даже сотни тысяч запросов в секунду.</span><span class="sxs-lookup"><span data-stu-id="a7de6-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="a7de6-325">Так вот почему текстовый ящик был пуст.</span><span class="sxs-lookup"><span data-stu-id="a7de6-325">So that's why the text box was empty.</span></span> <span data-ttu-id="a7de6-326">После отправки страницы ASP.NET создал новый экземпляр страницы и пробежал код и разметку.</span><span class="sxs-lookup"><span data-stu-id="a7de6-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="a7de6-327">В этом коде не было ничего, что говорило бы ASP.NET внести значение в текстовое поле.</span><span class="sxs-lookup"><span data-stu-id="a7de6-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="a7de6-328">Так что ASP.NET ничего не сделал, и текстовый ящик был представлен без значения в нем.</span><span class="sxs-lookup"><span data-stu-id="a7de6-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="a7de6-329">Там на самом деле простой способ обойти этот вопрос.</span><span class="sxs-lookup"><span data-stu-id="a7de6-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="a7de6-330">Жанр, который вы ввели в текстовое &mdash; поле *доступен* для вас в коде это в `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="a7de6-331">Обновление разметки для текстового окна так, что `value` атрибут получает свою ценность от, `searchTerm`как этот пример:</span><span class="sxs-lookup"><span data-stu-id="a7de6-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="a7de6-332">На этой странице можно также `value` установить `searchTerm` атрибут переменной, так как эта переменная также содержит введомый жанр.</span><span class="sxs-lookup"><span data-stu-id="a7de6-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="a7de6-333">Но использование `Request` объекта для `value` установки атрибута, как показано здесь, является стандартным способом выполнения этой задачи.</span><span class="sxs-lookup"><span data-stu-id="a7de6-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="a7de6-334">(Предполагая, что вы &mdash; даже хотите сделать это в некоторых ситуациях, вы можете сделать страницу *без* значений в полях.</span><span class="sxs-lookup"><span data-stu-id="a7de6-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="a7de6-335">Все зависит от того, что происходит с вашим приложением.)</span><span class="sxs-lookup"><span data-stu-id="a7de6-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="a7de6-336">Вы не можете "запомнить" значение текстового ящика, используемого для паролей.</span><span class="sxs-lookup"><span data-stu-id="a7de6-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="a7de6-337">Это будет дыра в безопасности, чтобы позволить людям заполнить поле паролей с помощью кода.</span><span class="sxs-lookup"><span data-stu-id="a7de6-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="a7de6-338">Запустите страницу снова, введите жанр, и нажмите **Поиск жанра**.</span><span class="sxs-lookup"><span data-stu-id="a7de6-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="a7de6-339">На этот раз вы не только видите результаты поиска, но текстовый ящик запоминает то, что вы ввели в прошлый раз:</span><span class="sxs-lookup"><span data-stu-id="a7de6-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Страница, показывающая, что текстовое окно "запомнило" предыдущую запись](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="a7de6-341">Поиск любого слова в заголовке</span><span class="sxs-lookup"><span data-stu-id="a7de6-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="a7de6-342">Теперь вы можете искать для любого жанра, но вы также можете искать название.</span><span class="sxs-lookup"><span data-stu-id="a7de6-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="a7de6-343">Трудно получить название точно правильно, когда вы ищете, так что вместо этого вы можете искать слово, которое появляется в любом месте внутри названия.</span><span class="sxs-lookup"><span data-stu-id="a7de6-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="a7de6-344">Для этого в S'L `LIKE` используется оператор и синтаксис:</span><span class="sxs-lookup"><span data-stu-id="a7de6-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="a7de6-345">Эта команда получает все фильмы, названия которых содержат "приключение".</span><span class="sxs-lookup"><span data-stu-id="a7de6-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="a7de6-346">При использовании `LIKE` оператора вы включаете `%` символ подстановочного знака как часть поискового термина.</span><span class="sxs-lookup"><span data-stu-id="a7de6-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="a7de6-347">Поиск `LIKE 'adventure%'` означает "начало с "приключения".</span><span class="sxs-lookup"><span data-stu-id="a7de6-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="a7de6-348">(Технически, это означает "строка' приключение", а затем что-нибудь.") Аналогичным образом, `LIKE '%adventure'` термин поиска означает "все, что следует строка'приключение", который является еще одним способом сказать "окончание с" приключения ".</span><span class="sxs-lookup"><span data-stu-id="a7de6-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="a7de6-349">Таким образом, термин `LIKE '%adventure%'` поиска означает "приключение" в любом месте заголовка".</span><span class="sxs-lookup"><span data-stu-id="a7de6-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="a7de6-350">(Технически, "все в названии, а затем" приключения ", а затем ничего.")</span><span class="sxs-lookup"><span data-stu-id="a7de6-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="a7de6-351">Внутри `<form>` элемента добавьте следующую разметку прямо под тегом `</div>` закрытия `</form>` для поиска жанра (незадолго до элемента закрытия):</span><span class="sxs-lookup"><span data-stu-id="a7de6-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="a7de6-352">Код для обработки этого поиска похож на код для поиска жанра, за исключением того, что вы должны собрать `LIKE` поиск.</span><span class="sxs-lookup"><span data-stu-id="a7de6-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="a7de6-353">Внутри блока кода в верхней части `if` страницы, `if` добавить этот блок сразу после блока для жанра поиска:</span><span class="sxs-lookup"><span data-stu-id="a7de6-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="a7de6-354">Этот код использует ту же логику, которую `LIKE` вы видели ранее, за исключением того, что поиск использует оператора и код ставит "`%`до и после термина поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="a7de6-355">Обратите внимание, как было легко добавить еще один поиск на страницу.</span><span class="sxs-lookup"><span data-stu-id="a7de6-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="a7de6-356">Все, что вам нужно было сделать, это:</span><span class="sxs-lookup"><span data-stu-id="a7de6-356">All you had to do was:</span></span>

- <span data-ttu-id="a7de6-357">Создайте `if` блок, который тестируется, чтобы увидеть, имеет ли значение соответствующее поле поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="a7de6-358">Установите `selectCommand` переменную на новую выписку S'L.</span><span class="sxs-lookup"><span data-stu-id="a7de6-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="a7de6-359">Установите `searchTerm` переменную к значению для передачи в запрос.</span><span class="sxs-lookup"><span data-stu-id="a7de6-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="a7de6-360">Вот полный блок кода, который содержит новую логику для поиска заголовков:</span><span class="sxs-lookup"><span data-stu-id="a7de6-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="a7de6-361">Если вкратце, этот код выполняет указанные ниже действия.</span><span class="sxs-lookup"><span data-stu-id="a7de6-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="a7de6-362">Переменные `searchTerm` имитируются `selectCommand` в верхней части.</span><span class="sxs-lookup"><span data-stu-id="a7de6-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="a7de6-363">Вы собираетесь установить эти переменные на соответствующий термин поиска (если таковые имеется) и соответствующую команду S'L на основе того, что пользователь делает на странице.</span><span class="sxs-lookup"><span data-stu-id="a7de6-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="a7de6-364">Поиск по умолчанию является простым случаем получения всех фильмов из базы данных.</span><span class="sxs-lookup"><span data-stu-id="a7de6-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="a7de6-365">В тестах `searchGenre` `searchTitle`для и, `searchTerm` код устанавливает значение, вы хотите искать.</span><span class="sxs-lookup"><span data-stu-id="a7de6-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="a7de6-366">Эти блоки `selectCommand` кода также устанавливаются в соответствующую команду S'L для этого поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="a7de6-367">Метод `db.Query` вызывается только один раз, используя все, что команда S'L находится в `selectedCommand` и независимо от значения в `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="a7de6-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="a7de6-368">Если нет термина поиска (нет жанра и `searchTerm` нет заголовка слова), значение является пустой строкой.</span><span class="sxs-lookup"><span data-stu-id="a7de6-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="a7de6-369">Однако это не имеет значения, так как в этом случае запрос не требует параметра.</span><span class="sxs-lookup"><span data-stu-id="a7de6-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="a7de6-370">Тестирование функции поиска заголовка</span><span class="sxs-lookup"><span data-stu-id="a7de6-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="a7de6-371">Теперь вы можете протестировать завершенную страницу поиска.</span><span class="sxs-lookup"><span data-stu-id="a7de6-371">Now you can test your completed search page.</span></span> <span data-ttu-id="a7de6-372">Выполнить *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="a7de6-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="a7de6-373">Введите жанр и нажмите **Поиск жанра**.</span><span class="sxs-lookup"><span data-stu-id="a7de6-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="a7de6-374">Сетка отображает фильмы этого жанра, как и раньше.</span><span class="sxs-lookup"><span data-stu-id="a7de6-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="a7de6-375">Введите заголовок слова и нажмите **Кнопка Поиск Название**.</span><span class="sxs-lookup"><span data-stu-id="a7de6-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="a7de6-376">Сетка отображает фильмы, которые имеют это слово в названии.</span><span class="sxs-lookup"><span data-stu-id="a7de6-376">The grid displays movies that have that word in the title.</span></span>

![Фильмы страницы листинг после поиска 'The' в названии](form-basics/_static/image6.png)

<span data-ttu-id="a7de6-378">Оставьте оба текстовых ящика пустыми и нажмите на любой из кнопок.</span><span class="sxs-lookup"><span data-stu-id="a7de6-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="a7de6-379">Сетка отображает все фильмы.</span><span class="sxs-lookup"><span data-stu-id="a7de6-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="a7de6-380">Объединение запросов</span><span class="sxs-lookup"><span data-stu-id="a7de6-380">Combining the Queries</span></span>

<span data-ttu-id="a7de6-381">Вы можете заметить, что поиск, который вы можете выполнить, являются эксклюзивными.</span><span class="sxs-lookup"><span data-stu-id="a7de6-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="a7de6-382">Вы не можете искать название и жанр в то же время, даже если оба поиска коробки имеют значения в них.</span><span class="sxs-lookup"><span data-stu-id="a7de6-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="a7de6-383">Например, нельзя искать все боевики, название которых содержит "Приключение".</span><span class="sxs-lookup"><span data-stu-id="a7de6-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="a7de6-384">(Поскольку страница закодирована сейчас, если вы вводите значения как для жанра, так и для заголовка, поиск заголовка получает приоритет.) Для создания поиска, сочетающего в себе условия, необходимо создать запрос s-L, который имеет синтаксис, как следующее:</span><span class="sxs-lookup"><span data-stu-id="a7de6-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="a7de6-385">И вам придется запустить запрос, используя заявление, как следующее (примерно говоря):</span><span class="sxs-lookup"><span data-stu-id="a7de6-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="a7de6-386">Создание логики, позволяющей многим перестановкам критериев поиска, может быть немного вовлечено, как вы можете видеть.</span><span class="sxs-lookup"><span data-stu-id="a7de6-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="a7de6-387">Поэтому мы остановимся на этом.</span><span class="sxs-lookup"><span data-stu-id="a7de6-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="a7de6-388">Далее</span><span class="sxs-lookup"><span data-stu-id="a7de6-388">Coming Up Next</span></span>

<span data-ttu-id="a7de6-389">В следующем уроке вы создадите страницу, которая использует форму, чтобы позволить пользователям добавлять фильмы в базу данных.</span><span class="sxs-lookup"><span data-stu-id="a7de6-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="a7de6-390">Полный список страницы фильма (обновлено с поиском)</span><span class="sxs-lookup"><span data-stu-id="a7de6-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="a7de6-391">Дополнительные ресурсы</span><span class="sxs-lookup"><span data-stu-id="a7de6-391">Additional Resources</span></span>

- [<span data-ttu-id="a7de6-392">Введение в веб-программирование для ASP.NET с использованием синтаксиса Razor</span><span class="sxs-lookup"><span data-stu-id="a7de6-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="a7de6-393">[Пункт ОДД](http://www.w3schools.com/sql/sql_where.asp) на сайте W3Schools</span><span class="sxs-lookup"><span data-stu-id="a7de6-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="a7de6-394">[Статья определения методов](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) на сайте W3C</span><span class="sxs-lookup"><span data-stu-id="a7de6-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="a7de6-395">[Назад](displaying-data.md)
> [Вперед](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="a7de6-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
